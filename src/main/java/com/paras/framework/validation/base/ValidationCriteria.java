package com.paras.framework.validation.base;

/**
 * Validation Criteria to convey the validation criteria value has failed particularly.
 
 * @Author Paras.
 */
public enum ValidationCriteria {
    MANDATORY, REGEX, MIN_LENGTH, MAX_LENGTH
}